


### Setup
```bash
git clone ... optimum-intel
cd optimum-intel
pip install -e .

pip install nncf openvino
pip install tabulate # as needed in qa's requirements.txt, we manually do this.

pip install torchvision
```